version: '3.8'
services:
  app:
    build: .
    ports:
      - "3000:3000"
  web:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./index.html:/usr/share/nginx/html/index.html
